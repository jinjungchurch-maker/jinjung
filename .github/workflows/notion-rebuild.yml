name: 노션 자동 반영

on:
  schedule:
    - cron: '0 21,3,9 * * *'   # 한국시간 오전 6시·정오·오후 6시 (UTC 기준)
  workflow_dispatch:            # 수동 실행 버튼도 생김

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Netlify 재빌드 요청
        run: curl -fsS -X POST "${{ secrets.NETLIFY_BUILD_HOOK }}"
